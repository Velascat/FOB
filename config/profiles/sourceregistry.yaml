name: SourceRegistry
repo_root: /home/dev/Documents/GitHub/SourceRegistry

claude:
  continue: true
  bootstrap_files: []
  peers: []

panes:
  git:
    command: lazygit
  shell:
    command: bash

helpers:
  test: pytest -q
  audit: ruff check .
